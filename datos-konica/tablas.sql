/*CREAR BASE DE DATOS*/
CREATE DATABASE IF NOT EXISTS prb_impresiones;
GRANT ALL PRIVILEGES ON prb_impresiones.* TO 'desarrollo'@'%' IDENTIFIED BY 'Pow$579.d' WITH GRANT OPTION;

CREATE TABLE IF NOT EXISTS imps_A1UG011006937(
  con_id INTEGER unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY,
  user_name Name VARCHAR(50) DEFAULT 'SIN ASIGNAR',
  total_counter INTEGER UNSIGNED DEFAULT 0,
  total_counter INTEGER UNSIGNED DEFAULT 0,
  
  
  articulo VARCHAR(50) DEFAULT 'SIN ASIGNAR',
  ejercicio INTEGER UNSIGNED DEFAULT 1900,
  importe FLOAT (30,15) DEFAULT 000000000000000.000000000000000,
  cantidad FLOAT (30,15) DEFAULT 000000000000000.000000000000000,
  presentacion VARCHAR(50) DEFAULT 'SIN ASIGNAR',
  codigo_agente VARCHAR(50) DEFAULT 'SIN ASIGNAR',
  nombre_agente VARCHAR(100) DEFAULT 'SIN ASIGNAR',
  codigo_director VARCHAR(50) DEFAULT 'SIN ASIGNAR',
  nombre_director VARCHAR(100) DEFAULT 'SIN ASIGNAR',
  fecha_rem_est VARCHAR(12) DEFAULT '31/12/1900', 
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    ON UPDATE CURRENT_TIMESTAMP
);